model MlAuth {
  id           Int      @id @default(autoincrement())
  mlUserId     BigInt   @unique
  accessToken  String
  refreshToken String
  expiresAt    DateTime
  createdAt    DateTime @default(now())
}
